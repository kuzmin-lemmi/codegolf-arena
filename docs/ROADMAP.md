# План и состояние проекта

Это «память» проекта для следующего ассистента или разработчика.
**Закрыли задачу — отметьте её здесь в том же коммите.**

_Состояние на 23 сентября 2026._

**Новое направление (22–23 сентября): арена на трёх языках — Python, JavaScript,
C#.** Решения владельца и этапы — в [three-languages.md](three-languages.md).
Сервер увеличен до 2 ядер / 4 ГБ. Идёт этап 1 — фундамент.

---

## Сделано

**Бэкапы.** Скрипты копии, проверки и восстановления базы; проверены на живой
базе. Ночная копия в 03:30. Шифрованная отправка копий в хранилище S3 —
код готов и проверен, но само хранилище ещё не настроено (см. ниже).

**Сервер.** Timeweb VPS, домен `codegolf.ru`, HTTPS, служба сайта, nginx,
firewall, подкачка. Установка с нуля — [server-setup.md](server-setup.md).

**Честность соревнования.** Решение больше не видит данных тестов: раньше
`test["expected"]` проходил любую задачу. Регрессионная проверка
`npm run test:sandbox` стоит в предеплойных проверках. Страница задачи больше
не отдаёт браузеру скрытые тесты — только их количество (22 сентября).
На боевом раннере подтверждено: честное решение проходит, списывание — 0 тестов.

**Проба C#** (22 сентября) — [csharp-trial.md](csharp-trial.md). У 12 задач можно
решать и на C#: своя таблица рекордов, вне очков и общего рейтинга, включается
`CSHARP_ENABLED` — **на сайте включено 22 сентября**. mono стоит в боевом раннере. Честность C#-раннера —
`npm run test:sandbox:csharp` (88 проверок), он тоже в предеплойных проверках.

**Вход через Stepik** работает. Исправлены кука сессии и подбор свободного ника.

**Продуктовые улучшения** — подробно в [product-stage4.md](product-stage4.md),
там они названы «этап 4». Очки за укорачивание решения и за первое место,
уведомления «рекорд побит», цель по длине, история попыток, подсчёт зачёта
соревнований, публичные страницы участников с картинкой для соцсетей.

**Устойчивость:**

- деплой больше не затирает правки задач из админки;
- структура базы меняется миграциями;
- Python 3.11 и в браузере, и в раннере;
- сайт не стартует без адреса раннера;
- у зависших проверок не больше 3 попыток, у запасной проверки — 15 секунд на всё;
- IP посетителя нельзя подделать заголовком;
- Next.js 15.5.25: закрыты критические уязвимости;
- инструкции по мониторингу и восстановлению из хранилища.

---

## Срочное — нашли при анализе 22 сентября

- [x] **Скрытые тесты видны на странице задачи** — исправлено 22 сентября:
  страница берёт только открытые тесты (`where: { isHidden: false }`), о скрытых
  сообщает лишь их количество. Проверено сквозной проверкой: аргумент скрытого
  теста в HTML страницы больше не попадает.
- [ ] **Девять задач без скрытых тестов**, у `running-sum` и `rotate` — один
  открытый тест: рекорд ставится подгонкой ответа. Список — в [plan.md](plan.md),
  пункт 2.2.

---

## Открытые задачи — за владельцем

- [x] ~~Итоги пробы C# 6 октября~~ — не ждём: владелец 22 сентября решил делать
  арену на трёх языках ([three-languages.md](three-languages.md)), сервер увеличен
  до 2 ядер / 4 ГБ 23 сентября.
Здесь нужны его учётные записи или права root.

- [ ] **root:** обновить файлы ночного расписания — теперь оно ещё и отправляет
  копию наружу:
  `cp /home/deploy/codegolf-arena/scripts/systemd/codegolf-backup.* /etc/systemd/system/ && systemctl daemon-reload`
- [ ] **Хранилище S3** в панели Timeweb и файл `~/.config/codegolf/offsite.env` —
  по [backup.md](backup.md). Пароль шифрования — в менеджер паролей.
  После этого ИИ проверяет отправку (`bash scripts/backup-offsite.sh`)
  и восстановление (`bash scripts/fetch-offsite-backup.sh`).
- [ ] **UptimeRobot** на `https://codegolf.ru/api/health` — [backup.md](backup.md), «Мониторинг».
- [ ] **Healthchecks.io** для ночных бэкапов — его адрес вписать в `HEALTHCHECK_URL`.
- [ ] **Сменить пароль администратора** — он засветился в переписке с ИИ.
- [ ] **Назначить задачу недели** в админке — демонстрационная «Фибоначчи»
  истекает около 24 сентября 2026.
- [ ] Удалить на stepik.org/oauth2/applications лишние приложения, если остались;
  рабочее — с ID `1fuMPVCI…`.

---

## Дальше по плану

Подробный разбор с цифрами, приоритетами и оценками — **[plan.md](plan.md)**.
Коротко, по этапам:

1. **Этап 5. Честный запуск** (2–3 дня): закрыть срочное выше, не дать опустеть
   задаче недели, переписать условия бронзовых задач, закрыть задачи владельца,
   пригласить первые 10–20 человек. Перед этим — живая проверка по
   [server-setup.md](server-setup.md), шаг 8: регистрация, решение, рейтинг,
   контрольная попытка `test["expected"]`.
2. **Этап 6. Возврат игроков** (4–6 дней): Telegram-бот с уведомлением «рекорд
   побит», воронка и отчёт в админке, Sentry, вводные задачи с разборами,
   решение про почту.
3. **Этап 7. Надёжность** (2–3 дня): автопроверки в GitHub Actions на каждый push
   (`tsc`, `lint`, `build`, `test:sandbox`, `check:rules`), нагрузочная проверка
   на 30 одновременных отправок, условия серебряных задач, чистка мёртвого кода.
4. **Этап 8. Рост** (по желанию): группы и классы для преподавателей, ачивки и
   серии, страницы «как решить X в одну строку», дуэли 1×1, другие языки.

---

## Известные ограничения и технический долг

- **`postcss` внутри Next** — уязвимость только при сборке, с собственными стилями
  проекта. Лечится переходом на Next 16 (мажорное обновление).
- **Очередь проверок живёт в памяти процесса** — при перезапуске сайта
  незавершённые задания теряются. Лимиты запросов умеют работать через Redis
  (`RATE_LIMIT_REDIS_URL`), но он не настроен и на одном сервере не нужен.
  Для нескольких серверов понадобятся Redis и отдельный воркер.
- **Pyodide 0.24.1 устарел.** Обновление = смена версии Python везде
  (правило 3 в [AGENTS.md](../AGENTS.md)). Python 3.12 дал бы удобные
  для кодгольфа f-строки (PEP 701).
- **Скрипты хранилища S3 проверены с подменой хранилища.** На настоящем —
  после настройки.
- **Вход по почте без восстановления пароля** (решение владельца 23 сентября —
  исправить позже): забыл пароль — потерял аккаунт, подтверждения email тоже нет.
  Варианты — оставить только вход через Stepik или подключить отправку писем
  (сброс и подтверждение) — [plan.md](plan.md), пункт 3.5.
- Уведомления — только на сайте, без почты и Telegram.
- **Нет автопроверок на push** (папки `.github` нет): `tsc`, `lint`, `build`,
  `test:sandbox`, `check:rules` запускаются только руками.
- **Не видно, что происходит:** ни счётчика посещений, ни сбора ошибок.
  Логи nginx пользователю `deploy` недоступны (нужна группа `adm`, шаг от root).
- Мёртвый код: `src/lib/mock-data.ts`, дубль `validateOneliner`
  в `src/lib/python-serializer.ts` (рабочий — в `src/lib/utils.ts`),
  стили `.monaco-editor` в `src/app/globals.css`.
